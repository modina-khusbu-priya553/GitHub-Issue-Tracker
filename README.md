1️⃣ What is the difference between var, let, and const?
is= The difference between thems are: 
Var is scoped function where redeclearetion is possibe and we can reasigned values.Var can hoisting but initialized as undefined. 
Let is Block-scoped reassignment allowed but it's not possible to redecleare a value. Let is hoisted too but it is not initialized immediately because of temporal dead zone.
Const is a Block-scoped as well but there is not allowed to redeclearetion and reassignment values, and const can be  hoisted but it is not initialized immediately because of temporal dead zone.

2️⃣ What is the spread operator (...)?
Spread operator is used to spread out or copy values from an array or object.

3️⃣ What is the difference between map(), filter(), and forEach()?
map() is used to change items easily and create a new array where filter() used to select certain items from an array and return only the item match the condition. 
forEach() only used to loop through an array not work in object. It does something for each item, but does not create a new array.

4️⃣ What is an arrow function?
An arrow function is a shorter way to write a function in JavaScript.

5️⃣ What are template literals?
It is modern way to write code in js. We use (` `) backticks instead of quotes and write variables inside strings and multi-line text easilly.
